<div class="document-container">

    <div class="row" id="index">
      <% documents.each do |document| %>
        <div class="col-lg-6 col-md-6 col-sm-12">
          <div class="card doc-card">
            <% if document.document_image_id.present? %>
                <%= attachment_image_tag document, :document_image,
                    class: "card-img-top doc-img", id: "imageArea" %>
            <% else %>
              <div class="stage">
                <img src="/assets/stage/stage<%= (doc_number(document.goal_id, document.id)/5) + 1 %>.jpg"
                     class="stage-img" alt="stage-image">
                <!--%= image_tag '', class: "dungeon-img" %-->
                <% if doc_number(document.goal_id, document.id) % 5 == 4 %>
                  <div class="boss-monster">
                    <img src="/assets/monster/boss<%= doc_number(document.goal_id, document.id) / 4 %>.png"
                         class="monster-img" alt="monster-image">
                  </div>
                <% else %>
                  <div class="monster-left">
                    <img src="/assets/monster/monster<%= doc_number(document.goal_id, document.id) %>.png"
                         class="monster-img" alt="monster-image">
                  <!--%= image_tag '', class: "monster-img" %-->
                  </div>
                  <div class="monster-right">
                    <img src="/assets/monster/monster<%= doc_number(document.goal_id, document.id) + 1 %>.png"
                         class="monster-img" alt="monster-image">
                  <!--%= image_tag '', class: "monster-img" %-->
                  </div>
                <% end %>
              </div>
            <% end %>
            <div class="card-body doc-body">
              <div class="row">
                <p class="doc-title">
                  <%= cat_name(document.goal_id) %>のダンジョン( <span><%= doc_number(document.goal_id, document.id) %></span> F )にいどんだ！
                </p>
                <p class="doc-body">
                  <% if doc_number(document.goal_id, document.id) % 5 == 4 %>
                    ボスがあらわれた！</br>
                    <%= document.body %> </br>
                    <%= user.name %>はボスをたおし、 </br>
                    <%= document.add_level %> レベルアップした！
                  <% else %>
                    <%= document.body %> </br>
                    <%= user.name %>は <%= document.add_level %> レベルアップした！
                  <% end %>
                </p>
                <p class="doc-body">
                  【つぎのもくひょう】　</br>
                  <%= document.milestone %> </br>
                  <% unless cat_level(document.goal_id) >= 100 %>
                    せかいめつぼうまで　のこり <%= timelimit(document.goal_id, document) %>  にち
                  <% end %>
                </p>
                <p class="doc-bottom">
                  <% if current_user.id == document.user_id %>
                    <span class="link">
                      <%= link_to 'へんしゅう', edit_document_path(document.id) %> ｜
                      <%= link_to 'さくじょ', document_path(document.id), method: :delete,
                          "data-confirm" => "削除してよろしいですか？" %>
                    </span>
                  <% end %>
                    <span class="doc-date">
                      <span class="doc-month"><%= document.created_at.strftime('%m') %></span>
                      <span class="doc-day"><%= document.created_at.strftime('%d') %></span>
                    </span>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
<%= paginate documents %>
</div>